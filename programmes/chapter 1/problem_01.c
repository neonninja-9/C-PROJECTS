#include <stdio.h>

int main(){
    //Write a C program to calculate area of a rectangle:
    //a. Using hard coded inputs.
    int length = 10;
    int weight = 20;
    printf("the area of rectrangle is %d", length*weight);
    return 0;
}