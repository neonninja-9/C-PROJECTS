#include <stdio.h>
int main(){
    //b. Using inputs supplied by the user.
    int length, weight;
    printf("enter the length of rectrangle\n");
    scanf("%d", &length);
    printf("enter the leanght weight of rectrangle\n");
    scanf("%d", &weight);
    printf("the area of rectrangle is %d", length*weight);
    return 0;
}